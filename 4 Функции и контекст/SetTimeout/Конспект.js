/*
========================================
⏱ setTimeout и setInterval — КОНСПЕКТ
========================================

📌 setTimeout и setInterval — это НЕ часть JS-движка.
Они предоставляются средой выполнения:
- в браузере → Web API
- в Node.js / Bun → таймеры среды

JS сам по себе однопоточный.
Таймеры работают через EVENT LOOP.

----------------------------------------
🧠 ОБЩАЯ МЕНТАЛЬНАЯ МОДЕЛЬ
----------------------------------------

1. JS выполняет код СИНХРОННО
2. Когда встречает setTimeout / setInterval:
   → передаёт колбэк в среду (браузер / Node)
3. Таймер отсчитывает время
4. После окончания:
   → колбэк попадает в очередь макрозадач (task queue)
5. Event Loop:
   → ждёт, пока Call Stack станет пустым
   → забирает задачу из очереди
   → выполняет её

ВАЖНО:
⛔ setTimeout НЕ гарантирует точное время
✅ он гарантирует МИНИМАЛЬНУЮ задержку

----------------------------------------
🕒 setTimeout
----------------------------------------

СИНТАКСИС:
setTimeout(callback, delay, arg1, arg2, ...)

callback — функция, которая выполнится позже
delay — минимальная задержка в миллисекундах
аргументы — передаются в callback

Пример:
*/
setTimeout(() => {
	console.log('Hello after 1 second');
}, 1000);

/*
📌 Что реально происходит:
- JS дошёл до setTimeout
- отдал callback браузеру
- пошёл выполнять код дальше
- через 1000ms callback попадёт в очередь
- выполнится, когда стек будет пуст

----------------------------------------
❗ setTimeout(..., 0)
----------------------------------------

Это НЕ "выполнить сразу".

setTimeout(fn, 0):
- откладывает выполнение
- fn выполнится ПОСЛЕ текущего синхронного кода

Пример:
*/
console.log('A');

setTimeout(() => {
	console.log('B');
}, 0);

console.log('C');

// Вывод: A → C → B

/*
----------------------------------------
🧨 clearTimeout
----------------------------------------

setTimeout возвращает ID таймера.
Его можно отменить.

Пример:
*/
const timerId = setTimeout(() => {
	console.log('Не выполнится');
}, 1000);

clearTimeout(timerId);

/*
----------------------------------------
🔁 setInterval
----------------------------------------

setInterval — выполняет callback КАЖДЫЙ раз
через указанный интервал времени.

СИНТАКСИС:
setInterval(callback, delay)

Пример:
*/
const intervalId = setInterval(() => {
	console.log('Tick');
}, 1000);

/*
⚠️ ВАЖНО:
Если callback выполняется ДОЛГО,
интервалы НЕ накапливаются.
Следующий запуск ждёт освобождения стека.

----------------------------------------
🧨 clearInterval
----------------------------------------

Останавливает интервал.

Пример:
*/
clearInterval(intervalId);

/*
----------------------------------------
🚨 ПРОБЛЕМА setInterval
----------------------------------------

Если callback тяжёлый:
- интервалы могут "плыть"
- трудно контролировать остановку

Поэтому на практике ЧАЩЕ используют рекурсивный setTimeout.

----------------------------------------
🔄 Рекурсивный setTimeout (РЕКОМЕНДУЕТСЯ)
----------------------------------------

Плюсы:
✅ точный контроль
✅ следующий запуск только после завершения предыдущего
✅ проще останавливать

Пример:
*/
function tick() {
	console.log('Tick');

	setTimeout(tick, 1000);
}

setTimeout(tick, 1000);

/*
----------------------------------------
🧠 СРАВНЕНИЕ
----------------------------------------

setInterval:
- прост
- плохо контролируется
- может "ехать" по времени

Рекурсивный setTimeout:
- чуть сложнее
- надёжнее
- используется в реальных проектах

----------------------------------------
📦 Где используется
----------------------------------------

- debounce / throttle
- polling (опрос сервера)
- анимации
- таймеры
- отложенные действия
- retry логика

----------------------------------------
🧠 КЛЮЧЕВЫЕ МЫСЛИ (запомнить)
----------------------------------------

✔ JS не ждёт setTimeout
✔ setTimeout ≠ точное время
✔ Таймеры → очередь → event loop
✔ setTimeout(0) — отложить выполнение
✔ В проде часто заменяют setInterval на рекурсивный setTimeout

========================================
КОНЕЦ КОНСПЕКТА
========================================
*/

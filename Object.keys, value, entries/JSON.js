// JS-объект — это живая структура в памяти программы
// JSON — это фото этого объекта, сохранённое в текстовом виде

// Фото можно:
// передать по интернету 🌍
// сохранить в файл 💾
// отправить серверу 📡

// В JavaScript мы работаем с объектами и массивами.
// Но ❗ по сети нельзя передать JS-объект напрямую.

// По сети передаётся:
// строка
// байты

// 📌 Поэтому:
// JS-объект ➜ превращаем в строку (JSON)
// Отправляем
// На другой стороне ➜ превращаем строку обратно в объект
// JS объект
//    ↓ JSON.stringify
// JSON строка
//    ↓ интернет
// JSON строка
//    ↓ JSON.parse
// JS объект

// JSON — строгий, он не прощает ошибок 😈

// МОЖНО
// | Тип     | Пример         |
// | ------- | -------------- |
// | string  | `"hello"`      |
// | number  | `42`           |
// | boolean | `true / false` |
// | null    | `null`         |
// | object  | `{}`           |
// | array   | `[]`           |

// ❌ В JSON НЕЛЬЗЯ
// | Что               | Почему               |
// | ----------------- | -------------------- |
// | функции           | это не данные        |
// | undefined         | не существует в JSON |
// | комментарии       | это просто текст     |
// | одинарные кавычки | **только двойные**   |
// | висячие запятые   | JSON строгий         |

// JSON.stringify — объект ➜ строка
const user = {
	name: 'Katrin',
	age: 32,
};

const json = JSON.stringify(user);

console.log(json);
// '{"name":"Katrin","age":32}'

// JSON.parse — строка ➜ объект
const json2 = '{"name":"Katrin","age":32}';

const user2 = JSON.parse(json);

console.log(user.name); // Katrin

// 📌 Поэтому:
// JSON должен быть идеальным
// серверы часто падают из-за одной лишней запятой 😅

// Где ты будешь видеть JSON в реальной жизни:

// fetch / api
fetch('/api/user')
	.then(res => res.json())
	.then(data => {
		console.log(data.name);
	});

// localStorage
localStorage.setItem('user', JSON.stringify(user));

const savedUser = JSON.parse(localStorage.getItem('user'));
// 🔹 Backend ↔ Frontend
// Сервер почти всегда отвечает JSON.

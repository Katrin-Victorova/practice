// HTTP — ОСНОВЫ (конспект)

/*
HTTP (HyperText Transfer Protocol) — это ПРОТОКОЛ передачи данных.
Проще:
HTTP описывает ПРАВИЛА, по которым
👉 клиент (обычно браузер или JS-код)
👉 и сервер (сайт, API) обмениваются сообщениями.
*/

/*
⚠️ ВАЖНОЕ СВОЙСТВО HTTP
HTTP — протокол БЕЗ СОСТОЯНИЯ (stateless)
Это значит:
- сервер НЕ помнит предыдущие запросы
- каждый запрос обрабатывается как НОВЫЙ

Если нужно "помнить пользователя":
используются cookies, headers, tokens, sessions
(но это уже надстройки НАД HTTP)
*/

// КАК РАБОТАЕТ HTTP-ЗАПРОС (ШАГ ЗА ШАГОМ)
/* 
1️⃣ Пользователь:
- открывает сайт
- нажимает кнопку
- скроллит
- JS вызывает fetch / axios
2️⃣ Браузер или JS-код:
- формирует HTTP-запрос
  (method, url, headers, body)
3️⃣ Запрос:
- отправляется на сервер
4️⃣ Сервер:
- принимает запрос
- обрабатывает логику
- формирует HTTP-ответ
5️⃣ Браузер:
- получает ответ
- отображает данные пользователю
*/

/* 💡 Это происходит ОЧЕНЬ часто:
- десятки и сотни HTTP-запросов в секунду
- почти каждое действие пользователя = новый запрос */

// HTTP vs HTTPS
/*
| Параметр              | HTTP                     | HTTPS                      |
|-----------------------|--------------------------|----------------------------|
| Шифрование данных     | ❌ Нет                   | ✅ Есть (SSL / TLS)        |
| Порт                  | 80                       | 443                        |
| Безопасность          | Уязвим                   | Защищён                    |
| Тип передачи данных   | Открытый текст           | Зашифрованный канал        |
| Когда использовать    | Локальная разработка     | Всегда в продакшене        | */

/* 🔐 HTTPS = HTTP + SSL/TLS
То есть:
- структура запросов та же
- НО данные шифруются
- никто по дороге не может прочитать логины, пароли, токены */

/* ❗ В реальных проектах:
- браузеры ругаются на HTTP
- API почти всегда работают через HTTPS
- в продакшене HTTP использовать НЕЛЬЗЯ */

// КОРОТКОЕ ПРАВИЛО ДЛЯ ЗАПОМИНАНИЯ

/* HTTP — правила общения клиента и сервера.
HTTPS — то же самое, но в защищённом туннеле. */

// СТРУКТУРА HTTP-ЗАПРОСА

/* Пример HTTP-запроса в «сыром» виде:
GET /products HTTP/1.1
Host: example.com
User-Agent: Mozilla/5.0 (Windows NT 10.0)
Accept: application/json
Authorization: Bearer abc123 */

// 1. СТАРТОВАЯ СТРОКА (Request Line)

/* Синтаксис стартовой строки:
<HTTP-метод> <путь к ресурсу> <версия протокола> */

// Пример: GET /products HTTP/1.1

/* Разбор:
1️⃣ GET
— HTTP-метод
— говорит серверу, что мы хотим ПОЛУЧИТЬ ресурс

Существуют методы: GET, POST, PUT, DELETE и др. */

/*
2️⃣ /products
— путь на сервере
— условно: папка / файл / ресурс API
*/

/*
3️⃣ HTTP/1.1
— версия протокола
— сейчас чаще используется HTTP/2 (он работает быстрее)
*/

// 2. ЗАГОЛОВКИ ЗАПРОСА (Headers)

/* Заголовки — это ДОПОЛНИТЕЛЬНЫЕ инструкции для сервера.
Пользователь их не видит, но сервер по ним понимает:
1️⃣ кто мы
2️⃣ что мы хотим
3️⃣ в каком формате
4️⃣ как нас авторизовать */

// Пример заголовков:

/* Host: example.com
— домен сайта

User-Agent: Mozilla/5.0
— информация о браузере / клиенте

Accept: application/json
— формат данных, который мы ХОТИМ получить

Authorization: Bearer abc123
— токен авторизации

Content-Type: application/json
— формат тела запроса (если тело есть)
*/

//  3. ТЕЛО ЗАПРОСА (Body)
/* Тело запроса передаётся ТОЛЬКО у некоторых методов:
— POST
— PUT
— PATCH */

/* Обычно тело содержит:
1️⃣ JSON
2️⃣ форму (form-urlencoded)
3️⃣ файл */

// Пример тела запроса (JSON):
/* {
  "username": "student",
  "password": "123456"
} */

/* ❗ ВАЖНО: Тело запроса ВСЕГДА описывается заголовком:
Content-Type: application/json */

// ПРИМЕР ЗАПРОСА ЧЕРЕЗ fetch
fetch(url, {
	method: 'POST', // или GET, PUT, DELETE и т.д.
	headers: {
		'Content-Type': 'application/json', // тип данных
		Authorization: 'Bearer token', // если нужен
		'X-Custom-Header': 'customValue', // свои заголовки тоже можно
	},
	body: JSON.stringify(data), // тело запроса (если нужно)
});

// HTTP-ОТВЕТ (ОТ СЕРВЕРА К КЛИЕНТУ)

/* Пример HTTP-ответа:
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 75

{
  "status": "success",
  "data": {
    "id": 1,
    "name": "MacBook Pro"
  }
}
*/

// СТАРТОВАЯ СТРОКА ОТВЕТА (Status Line)

/* Формат:
<версия протокола> <код состояния> <сообщение> */

/* Пример:
HTTP/1.1 200 OK */

/* Где:
— HTTP/1.1 → версия протокола
— 200 → код состояния (успех)
— OK → текстовое сообщение */

// КОРОТКОЕ МЕНТАЛЬНОЕ ПРАВИЛО
/* HTTP-запрос:
→ Request Line
→ Headers
→ Body (иногда)

HTTP-ответ:
→ Status Line
→ Headers
→ Body */

// 2. ЗАГОЛОВКИ ОТВЕТА (Response Headers)

/* Заголовки ответа определяют:
— формат данных
— кэширование
— сессии
— cookies
— и другие параметры ответа сервера */

// Примеры заголовков ответа:

/* Content-Type: application/json
— формат данных, которые сервер отправляет клиенту

Content-Length: 75
— длина тела ответа (в байтах)

Set-Cookie: sessionid=xyz
— установка cookie на стороне клиента */

// 3. ТЕЛО ОТВЕТА (Response Body)

/* Тело ответа — это САМИ ДАННЫЕ,
которые мы запрашивали у сервера
(или сообщение об ошибке). */

/* Пример тела ответа:
{
  "status": "success",
  "data": {
    "id": 1,
    "name": "MacBook Pro"
  }
} */

/* ❗ ВАЖНО:
— формат тела ответа всегда описывается заголовком Content-Type
— чаще всего это application/json */

// HTTP-КОДЫ ОТВЕТА (Status Codes)
/* HTTP-код ответа показывает:
— что произошло с запросом
— успешно или с ошибкой */

/*
| Код | Категория            | Значение                                   |
|-----|----------------------|---------------------------------------------|
| 1xx | Информационные       | "Ожидание продолжения"                      |
| 2xx | Успех                | 200 OK, 201 Created, 204 No Content          |
| 3xx | Редирект             | 301, 302, 304 Not Modified                  |
| 4xx | Ошибка клиента       | 400 Bad Request, 401 Unauthorized,           |
|     |                      | 403 Forbidden, 404 Not Found                 |
| 5xx | Ошибка сервера       | 500 Internal Server Error,                   |
|     |                      | 502 Bad Gateway, 503 Service Unavailable     | */

// МЕНТАЛЬНАЯ МОДЕЛЬ ДЛЯ ЗАПОМИНАНИЯ

/*
2xx — всё прошло хорошо
3xx — тебя перенаправили
4xx — ты сделал что-то не так
5xx — сервер сломался
*/

// ЧТО ТАКОЕ res.ok В JavaScript (fetch)
// res.ok — это свойство объекта Response, который возвращает fetch().

fetch('/api/data').then(res => {
	if (res.ok) {
		// Если ответ успешный (status 200–299)
		return res.json();
	} else {
		// Если ответ с ошибкой (404, 500 и т.д.)
		throw new Error('Что-то пошло не так: ' + res.status);
	}
});

// ЧТО ДЕЛАЕТ res.ok

/*
1️⃣ Возвращает true,
если HTTP-статус ответа находится в диапазоне:
200 – 299
2️⃣ Если статус:
404, 500, 401, 403 и т.д.
→ res.ok будет false
3️⃣ Позволяет:
— фильтровать ошибки
— обрабатывать их ВРУЧНУЮ
— не полагаться на автоматические исключения
*/

// ВАЖНО ПОНИМАТЬ
/* ❗ fetch НЕ считает 404 или 500 ошибкой Promise
Promise resolve-ится ВСЕГДА, если запрос дошёл до сервера. */

/* Поэтому:
— fetch успешно выполнился
— но сервер мог вернуть ошибку
— именно для этого и нужен res.ok */

// МЕНТАЛЬНАЯ МОДЕЛЬ
/* fetch → Promise resolved → проверяем res.ok

res.ok === true
→ можно читать тело ответа

res.ok === false
→ ошибка сервера или клиента
→ обрабатываем вручную
*/

/* ============================================================
   ОСНОВНЫЕ HTTP-ЗАГОЛОВКИ (CLIENT / SERVER)
   ============================================================ */

/* ============================================================
   1. Content-Type — тип содержимого в теле (клиент / сервер)
   ============================================================ */

/*
Этот заголовок сообщает,
в каком ФОРМАТЕ передаются данные
(в теле запроса или ответа).
*/

/*
— Клиент указывает Content-Type,
  когда отправляет тело запроса (POST, PUT и т.д.)
— Сервер указывает Content-Type,
  когда возвращает данные
*/

/*
Примеры:
Content-Type: application/json
Content-Type: text/html
Content-Type: multipart/form-data
*/

/* ============================================================
   2. Content-Length — размер тела в байтах (сервер)
   ============================================================ */

/*
Content-Length показывает,
СКОЛЬКО данных придёт в теле ответа.
*/

/*
Пример:
Content-Length: 128
*/

/*
Полезен для того,
чтобы клиент знал, сколько данных ожидать.
*/

/* ============================================================
   3. Accept — что клиент хочет получить в ответ (клиент)
   ============================================================ */

/*
Клиент говорит серверу:
"Я хочу ответ ВОТ В ТАКОМ формате".
*/

/*
Пример:
Accept: application/json
*/

/* ============================================================
   4. Authorization — заголовок авторизации (клиент)
   ============================================================ */

/*
Передаёт данные для аутентификации:
— токен доступа
— логин / пароль
— другие данные авторизации
*/

/*
Пример:
Authorization: Bearer eyJhbGciOi...
*/

/*
Этот заголовок очень часто используется
при работе с API.
*/

/* ============================================================
   5. User-Agent — кто делает запрос (клиент)
   ============================================================ */

/*
Содержит информацию о:
— браузере
— устройстве
— операционной системе
*/

/*
Пример:
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64)
*/

/*
Сервер может подстраивать ответ
под устройство или браузер.
*/

/* ============================================================
   6. Cookie — передача кук от клиента к серверу (клиент)
   ============================================================ */

/*
Если сервер ранее установил cookie,
браузер будет автоматически отправлять их
в каждом запросе.
*/

/*
Пример:
Cookie: session=xyz123; theme=dark
*/

/* ============================================================
   7. Set-Cookie — установка кук на клиенте (сервер)
   ============================================================ */

/*
Сервер говорит браузеру:
"Сохрани себе вот такую cookie".
*/

/*
Пример:
Set-Cookie: session=abc123; Path=/; HttpOnly; Secure
*/

/*
Cookies часто используются для:
— авторизации
— сессий
— языковых настроек
— пользовательских предпочтений
*/

/* ============================================================
   8. Content-Disposition — поведение при скачивании
   ============================================================ */

/*
Позволяет сказать браузеру:
"Скачай файл, а не открывай".
*/

/*
Пример:
Content-Disposition: attachment; filename="report.pdf"
*/

/* ============================================================
   9. Cache-Control — управление кэшем
   ============================================================ */

/*
Сервер указывает:
— можно ли кэшировать ответ
— и на какое время
*/

/*
Пример:
Cache-Control: max-age=3600, public
*/

// ======================================================
// DOM (Document Object Model)
// ======================================================

// DOM — это программная модель HTML-документа.
// Браузер превращает HTML в дерево объектов,
// с которым JavaScript может работать.

// ------------------------------------------------------
// 1. Что происходит под капотом?
// ------------------------------------------------------

// Когда браузер получает HTML:

// 1️⃣ Читает HTML-файл
// 2️⃣ Парсит (разбирает) его
// 3️⃣ Строит DOM-дерево
// 4️⃣ Каждый HTML-тег становится ОБЪЕКТОМ

// То есть HTML → превращается в JS-объекты

// ------------------------------------------------------
// 2. Как выглядит DOM-дерево?
// ------------------------------------------------------

/*

HTML:

<html>
  <body>
    <h1>Hello</h1>
    <p>Text</p>
  </body>
</html>

DOM структура:

Document
└── html
    ├── head
    │   └── title - "Example Page"
    └── body
        ├── h1 - "Hello, World!"
        ├── p - "This is an example of a simple HTML page."
        └── div
            └── p - "Another paragraph inside a div."


*/

// Это не просто разметка.
// Это иерархия ОБЪЕКТОВ в памяти браузера.

// ------------------------------------------------------
// 3. Типы узлов (Nodes)
// ------------------------------------------------------

// DOM состоит из узлов (nodes)

// 🔹 1. Document Node
// Корневой объект — document
// Это входная точка в DOM.

document; // глобальный объект DOM

// 🔹 2. Element Node
// Любой HTML-тег — это элемент

// <div>, <p>, <h1>, <body> — всё это Element Nodes

// 🔹 3. Text Node
// Текст внутри тегов — отдельный узел

// <h1>Hello</h1>
// "Hello" — это Text Node

// 🔹 4. Attribute Node
// Атрибуты элементов (id, class, href)

// ------------------------------------------------------
// 4. Как JavaScript получает доступ к DOM?
// ------------------------------------------------------

// Самый частый способ:

const element = document.getElementById('id');

// Другие способы:

document.querySelector('.class');
document.querySelectorAll('div');
document.getElementsByClassName('box');

// ------------------------------------------------------
// 5. Изменение текста
// ------------------------------------------------------

const title = document.getElementById('title');

// Меняем текст:
title.textContent = 'Новый текст';

// ⚠️ textContent — работает только с текстом
// Если нужно вставить HTML — используется innerHTML

// ------------------------------------------------------
// 6. Разница textContent vs innerHTML
// ------------------------------------------------------

// textContent
// 👉 работает только с текстом
// 👉 безопаснее

// innerHTML
// 👉 вставляет HTML
// 👉 может быть опасным (XSS)

// ------------------------------------------------------
// 7. Изменение стилей через JS
// ------------------------------------------------------

const box = document.getElementById('box');

// Меняем стиль:
box.style.backgroundColor = 'red';

// Важно:
// background-color → backgroundColor (camelCase)

// CSS: font-size
// JS: fontSize

// ------------------------------------------------------
// 8. Работа с атрибутами
// ------------------------------------------------------

const link = document.querySelector('a');

// Получить атрибут
link.getAttribute('href');

// Установить атрибут
link.setAttribute('href', 'https://google.com');

// ------------------------------------------------------
// 9. Работа с событиями (Events)
// ------------------------------------------------------

// DOM позволяет реагировать на действия пользователя

const button = document.getElementById('btn');

// Лучший способ:
button.addEventListener('click', function () {
	console.log('Кнопка нажата');
});

// Что происходит:
// 1️⃣ Пользователь кликает
// 2️⃣ Браузер создаёт событие
// 3️⃣ JS получает его
// 4️⃣ Выполняется функция

// ------------------------------------------------------
// 10. Добавление новых элементов
// ------------------------------------------------------

// Создание элемента:
const newDiv = document.createElement('div');

// Добавление текста:
newDiv.textContent = 'Я новый элемент';

// Добавление в DOM:
document.body.appendChild(newDiv);

// ------------------------------------------------------
// 11. Удаление элементов
// ------------------------------------------------------

const item = document.getElementById('item');
item.remove();

// ------------------------------------------------------
// 12. Как DOM хранится в памяти?
// ------------------------------------------------------

// DOM — это дерево объектов в памяти браузера.

// Когда ты меняешь DOM:
// → браузер делает Reflow (перерасчёт layout)
// → потом Repaint (перерисовка)

// Это может быть дорого по производительности.

// Поэтому:
// ❗ минимизируем частые изменения DOM
// ❗ стараемся делать изменения пакетами

// ------------------------------------------------------
// 13. Главная идея DOM
// ------------------------------------------------------

// DOM — это мост между:
// HTML (структура)
// CSS (стили)
// JavaScript (логика)

// Без DOM JavaScript не смог бы управлять страницей.

// ======================================================
// ВАЖНО ДЛЯ MIDDLE+
// ======================================================

// 🔥 DOM — это синхронный API
// 🔥 Любое изменение DOM может вызвать перерисовку
// 🔥 Работа с DOM — одна из самых дорогих операций

// Поэтому фреймворки (React, Vue) создают
// Virtual DOM — чтобы оптимизировать обновления.

// ======================================================
// КРАТКО
// ======================================================

// DOM = HTML → превращённый в дерево объектов
// Каждый тег = объект
// Мы можем:
// - читать
// - менять
// - удалять
// - создавать
// - слушать события

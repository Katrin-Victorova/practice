// Порядок:
// 1️⃣ Сначала возвращаем текущее значение.
// 2️⃣ Потом меняем переменную.

let x = 5;
let y = x++; // Шаг 1: y получает исходное x (y = 5)
// Шаг 2: x увеличивается на 1 (x = 6)
console.log(x, y); // 6 5

let a = 3;
let b = a--; // b = 3 → a = 2
console.log(a, b); // 2 3

// Порядок:
// 1️⃣ Сначала меняем переменную.
// 2️⃣ Потом возвращаем новое значение.

let x = 5;
let y = ++x; // Шаг 1: x увеличивается на 1 (x = 6)
// Шаг 2: y получает новое значение x (y = 6)
console.log(x, y); // 6 6

let a = 3;
let b = --a; // a = 2 → b = 2
console.log(a, b); // 2 2

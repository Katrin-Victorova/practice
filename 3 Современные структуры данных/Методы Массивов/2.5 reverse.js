// 2.5 reverse()
// Переворачивает элемент массива в обратном порядке
// Ничего не принимает
// Возвращает ссылку на исходный массив (мутирует его)
// Используеться для простого переворачивания массива
// Изменяет исходный массив
const arr = [1, 2, 3, 4];
console.log(arr);
console.log(arr.reverse());
// Создание объекта
const person = {
	name: 'Katrin',
	surname: 'Viktorova',
	favoriteNumber: 22,
};

const personInfo = `My name is ${person.name} ${person.surname} and my favorite numbers is ${person.favoriteNumber}`;

console.log(personInfo);

// функция должна проверить совпадает тип и переменная
function typeValidation(variable, type) {
  return typeof(variable) === type;
}

const typeValidation = (variable, type) => typeof(variable) === type;